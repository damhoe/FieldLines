<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <item name="popupMenuStyle">@style/AppTheme.PopupMenu.Primary</item>
        <item name="android:popupMenuStyle">@style/AppTheme.PopupMenu.Primary</item>
    </style>

    <style name="Widget.App.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">?attr/colorOnPrimary</item>
        <item name="titleTextColor">?attr/colorPrimary</item>
        <item name="navigationIconTint">?attr/colorPrimary</item>
        <item name="android:iconTint">?attr/colorPrimary</item>
    </style>

    <style name="Widget.App.ExtendedFloatingActionButton" parent="Widget.Material3.ExtendedFloatingActionButton.Secondary">

    </style>

    <style name="AppTheme.PopupMenu.Primary" parent="Widget.Material3.PopupMenu">
        <item name="android:popupElevation">8dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
